import {
    Box,
    Button,
    ButtonGroup,
    Container,
    Divider,
    Grid,
    Typography,
} from "@mui/material";
import React from "react";
import Hero from "../components/Hero";

const Home = () => {
    return (
        <Box>
            <Hero />
            <Container></Container>
        </Box>
    );
};

export default Home;
